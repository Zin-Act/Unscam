/*
if input contains (amazon or microsoft):{ 

      elseif (input contains "microsoft.com" or "amazon.com"):
          set text to ("real/legit")
      else:
          set text to ("fake do not click")
  else:
  set text to ("unknown")
}

psodo code lol this is called zinheerescript..... yeah.. toatally real
*/
